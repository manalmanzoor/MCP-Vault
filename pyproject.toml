[project]
name = "mcp-vault"
version = "0.1.0"
description = "A tiny personal MCP server for notes and bookmarks"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
